import React from 'react';
import PresentationPlayer from '../player/PresentationPlayer';

const Condition08_Medios: React.FC = () => {
    return (
        <PresentationPlayer sequenceId="seq-08" />
    );
};

export default Condition08_Medios;
